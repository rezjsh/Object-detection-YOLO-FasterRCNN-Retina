# Copy this file to `.env` and fill in your own values.

# Required for scripts/download_dataset.py — get a free key at https://roboflow.com
ROBOFLOW_API_KEY=your_roboflow_api_key_here

# Optional — only used if configs/train.yaml sets use_mlflow: true
MLFLOW_TRACKING_URI=file:./mlruns
